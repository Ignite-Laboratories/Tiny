package main

import (
	"fmt"
	"github.com/ignite-laboratories/tiny"
)

// TODO: Test this library

func main() {
	fmt.Println(tiny.Create.Random(8))
	fmt.Println(tiny.Create.Random(8))
	fmt.Println(tiny.Create.Random(8))
	fmt.Println(tiny.Create.Random(8))
	fmt.Println(tiny.Create.Random(8))
}
